services:
  - type: web
    name: flask-data-analysis
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app  # 替换为你的启动命令
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
